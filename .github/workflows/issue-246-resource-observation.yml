name: Issue 246 Resource Observation

on:
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  observe:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Observation paused while the harness update is published.'
