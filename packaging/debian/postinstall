#!/bin/sh
set -eu

if [ "${1:-configure}" != "configure" ]; then
	exit 0
fi

if command -v systemd-sysusers >/dev/null 2>&1; then
	systemd-sysusers /usr/lib/sysusers.d/podlaz.conf || true
fi

if command -v deb-systemd-helper >/dev/null 2>&1; then
	deb-systemd-helper unmask podlazd.service >/dev/null || true
	deb-systemd-helper enable podlazd.service >/dev/null || true
	deb-systemd-helper update-state podlazd.service >/dev/null || true
fi

if command -v deb-systemd-invoke >/dev/null 2>&1; then
	deb-systemd-invoke start podlazd.service >/dev/null || true
fi

exit 0
